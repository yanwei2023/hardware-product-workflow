# Hardware Product Workflow 自动接续说明

更新日期：2026-07-31  
GitHub：`git@github.com:yanwei2023/hardware-product-workflow.git`  
当前接续分支：`codex/agent-driven-lifecycle`

## 给另一台电脑上的 Codex

用户只需把本文件提供给你。请自行完成克隆、切换分支、安装、验证和启动，不要先要求用户手工执行 Git 命令。

优先使用 SSH：

```bash
git clone git@github.com:yanwei2023/hardware-product-workflow.git
cd hardware-product-workflow
git switch codex/agent-driven-lifecycle
```

如果 SSH 因没有密钥而失败，自动改用 HTTPS：

```bash
git clone --branch codex/agent-driven-lifecycle https://github.com/yanwei2023/hardware-product-workflow.git
cd hardware-product-workflow
```

若仓库已经存在，不要删除用户改动。先检查：

```bash
git status --short --branch
git remote -v
git fetch origin
git switch codex/agent-driven-lifecycle
git pull --ff-only origin codex/agent-driven-lifecycle
```

如工作区有未提交修改或无法快进，停止合并操作，保留现场并向用户说明冲突；不得使用 `git reset --hard` 或覆盖用户文件。远端分支 `origin/codex/agent-driven-lifecycle` 的最新提交是本说明的接续基准，可用下列命令核对：

```bash
git rev-parse HEAD
git rev-parse origin/codex/agent-driven-lifecycle
```

## 系统已落实的核心原则

该系统面向公司所有新产品开发及产品交付项目，不绑定 HFCT 或其他具体产品。

默认流程为：

```text
创建 S0 候选
→ Agent 自动执行 S0 工作包
→ 相应人员审核
→ S0 Gate 由项目负责人批准并冻结立项基线
→ Agent 生成项目正式配置蓝图
→ 项目负责人审核蓝图
→ 发布 S1-S10 正式流程
→ 自动排队 S1 Agent 工作
→ 后续每阶段均由 Agent 执行、人类审核、Gate 批准后推进
```

关键约束：

- 新项目创建时只生成 S0，不能预先生成 S1-S10。
- S0 立项定义包括项目类型、目标市场、客户场景、能力范围、技术范围、合规、供应、交付、运营和风险等级。
- S0 Gate 批准后冻结结构化立项基线。
- 项目蓝图由确定性编译器依据公司生命周期、100 份受控文档目录、项目类型和能力包生成。
- 蓝图必须由项目负责人批准后才能发布，重复批准不能重复创建正式流程。
- Agent 是默认执行者；人工负责人负责审核、批准、退回修改和阶段门决策。
- 退回修改会自动重新排队 Agent 工作；阶段门批准会自动派发下一阶段 Agent 工作。
- 旧 `POST /projects` 七阶段项目创建接口只用于兼容；默认接口是 `POST /projects/candidates`。

设计与实现依据：

- `docs/superpowers/specs/2026-07-31-s0-agent-driven-lifecycle-correction-design.md`
- `docs/superpowers/plans/2026-07-31-s0-agent-driven-lifecycle.md`
- `docs/project-creation.md`
- `docs/workflow-spine.md`
- `docs/agent-definitions.md`

## 新电脑安装与验证

要求 Node.js 20 或更高版本。进入仓库后执行：

```bash
node --version
npm install
npm run pilot:check
```

`pilot:check` 会完成：

- 全部 Node 测试和 React 生产构建；
- API smoke、store doctor 和运行时持久化检查；
- PostgreSQL schema、migration、导出、反向恢复、导入包和镜像预览检查；
- 旧 EVT 兼容演练；
- 默认 S0 候选到 S1 自动派工演练；
- 试点归档包生成和差异空白检查。

没有配置 `DATABASE_URL` 或未安装 `psql` 时，最后的 PostgreSQL 非严格 preflight 会报告 blockers，但命令仍可成功结束。这只表示可选 PostgreSQL 环境未就绪，不影响默认 JSON store 的局域网模拟试点。不要把这类提示误报为测试失败；同样，也不得在实际命令非零退出时声称验证通过。

可单独执行默认流程演练：

```bash
npm run pilot:rehearse:s0
```

预期关键结果：

```text
ok: true
phaseCount: 11
formalPhaseCount: 10
currentPhaseKey: s1_market_definition
s0AgentJobCount: 10
s0RequiredReviewCount: 8
s1QueuedAgentJobCount: 大于 0
```

## 局域网模拟测试

建议先设置访问码：

```bash
HARDWARE_FLOW_PILOT_ACCESS_CODE=请替换为内部试点访问码 npm run start:lan
```

默认端口为 `3001`。端口冲突时：

```bash
HOST=0.0.0.0 PORT=3191 HARDWARE_FLOW_PILOT_ACCESS_CODE=请替换为内部试点访问码 npm start
```

启动后在主机检查：

```text
http://localhost:3001/health
http://localhost:3001/ready
http://localhost:3001/runtime/network
http://localhost:3001/runtime/config
http://localhost:3001/ops/summary
http://localhost:3001/storage/doctor
```

`/runtime/network` 会给出推荐的局域网 URL。其他成员在同一局域网中通过浏览器打开该 URL，在工作台输入访问码。试点访问码只是局域网轻量保护，不是正式登录或 SSO。

建议的现场验收顺序：

1. 创建候选项目，确认只有 S0 和 10 个 Agent 任务。
2. 保存完整立项定义并处理 S0 Agent 队列。
3. 用绑定的人工负责人审核 S0 交付物。
4. 批准 S0 Gate，确认创建蓝图工作包。
5. 处理蓝图 Agent 任务，项目负责人批准蓝图。
6. 确认发布 S1-S10、当前阶段为 S1、S1 Agent 任务已排队。
7. 验证退回修改、证据、风险、通知、审计、阶段门审核包和下一阶段自动派工。

完整现场手册见 `docs/internal-pilot.md`，部署细节见 `docs/lan-deployment.md`。

## 数据、备份与恢复

默认运行使用：

```text
data/demo-store.json
```

该文件不会提交到 GitHub。局域网共享测试应由一台主机持续运行，其他电脑通过浏览器访问；不同电脑各自启动的 JSON 数据不会自动合并。

正式试点应把 `data` 目录放在持久化磁盘或 Docker volume。试点前在工作台创建检查点，并运行：

```bash
npm run store:doctor
```

数据异常时先暂停写操作，保留请求 ID 和现场，再检查 `/storage/doctor`。需要恢复 `.bak` 时：

```bash
npm run store:restore-backup
```

恢复会保留当前文件的 pre-restore 副本。不要直接删除 store 或覆盖备份。

## 当前已知边界

以下不应被描述为已完成：

- 外部大模型尚未接入；当前 Agent 队列、模板校验、确定性输出和人机审核编排是真实可运行的模拟层。
- 单份项目文档的等级手动调整、条件适用 N/A 和降级例外审批还没有形成完整 UI/API 操作闭环；100 份公司受控文档目录、默认分级和项目蓝图追踪已经存在。
- 没有正式用户登录、SSO、细粒度身份认证和生产级权限体系。
- 默认局域网试点使用 JSON store；PostgreSQL 已有迁移、校验、导入导出、启动快照和镜像桥，但不是默认完整 repository。
- 没有生产级 TLS、反向代理、数据库灾备、外部对象存储和高并发冲突处理。
- 飞书、企业微信和邮件通知尚未接入。

这些边界不阻止单机或局域网内的流程模拟测试，但阻止把当前版本宣称为生产就绪。

## 继续开发的纪律

- 保护 `main`，继续在 `codex/agent-driven-lifecycle` 或新的 `codex/...` 隔离分支/工作树工作。
- 不要把具体产品、HFCT 参数或某个项目名称写成平台默认配置。
- 新功能和缺陷修复先写失败测试，再实现，再跑完整验证。
- `main` 或 GitHub Issue 不能替代代码和正式状态证据；以当前远端分支、测试输出和文档为准。
- 不得把可选 PostgreSQL 环境缺失说成 JSON 试点失败，也不得把未实现边界说成完成。
- 修改完成后推送 GitHub，并确认远端分支 SHA 与本地 `HEAD` 一致。

